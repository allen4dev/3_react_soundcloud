import React from 'react';

import './index.css';

const Profile = () => (
  <section className="Profile">
    <h1 className="Profile-title">You are in Profile page</h1>
  </section>
);

export default Profile;
