import { NAME } from './constants';

export const SET_USER = `${NAME}/SET_USER`;
export const SET_USERS = `${NAME}/SET_USERS`;

export const SET_USER_TRACKS = `${NAME}/SET_USER_TRACKS`;
export const SET_NEXT_USER_TRACKS = `${NAME}/SET_NEXT_USER_TRACKS`;
