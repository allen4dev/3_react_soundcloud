import { NAME } from './constants';

export const SET_TRACK = `${NAME}/SET_TRACK`;
export const SET_TRACKS = `${NAME}/SET_TRACKS`;

export const SET_TRACK_COMMENTS = `${NAME}/SET_TRACK_COMMENTS`;
export const SET_NEXT_TRACK_COMMENTS = `${NAME}/SET_NEXT_TRACK_COMMENTS`;
